{
  "name": "quaderp-repo-root",
  "version": "0.0.0",
  "private": true,
  "description": "Tooling only. The application lives in store-app/client (Vercel) and store-app/server (Railway). Nothing here is deployed.",
  "scripts": {
    "start": "node scripts/not-the-app.js",
    "railway:plan": "railway config plan",
    "railway:apply": "railway config apply"
  },
  "devDependencies": {
    "railway": "^3.11.0"
  }
}
