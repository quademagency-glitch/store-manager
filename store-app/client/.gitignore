# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Throwaway build the app tour is captured from — it has the mock harness
# compiled in, which is exactly what must never reach dist/ (see
# docs/app-tour/README.md).
dist-tour
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Raw recordings, narration audio and intermediate cuts from scripts/record-*
# and scripts/generate-*. 88MB of media, and git history is permanent.
#
# Nothing is lost by ignoring it. The two videos that actually ship are
# committed in the landing repo at public/videos/walkthrough.mp4 and
# public/videos/theft-prevention.mp4 and served from there, and everything
# else here is reproducible by re-running the pipeline in scripts/.
walkthrough-recordings/
